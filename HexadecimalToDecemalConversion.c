#include <stdio.h>
int main(int argc, char const *argv[])
{
    int num;

    printf("enter hexadecimal numbers\n");
    scanf("%x",&num);

    printf("the hexidecimal of decimal num is %d\n",num);
    return 0;
}